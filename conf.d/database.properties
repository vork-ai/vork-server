# MongoDB connection
mongo.host=localhost
mongo.port=27017
mongo.database=vork

# Optional authentication — uncomment and fill in if your MongoDB requires credentials
# mongo.username=admin
# mongo.password=secret
